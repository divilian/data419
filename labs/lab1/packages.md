| Package       | Main workflow       | Typical artifacts                                                                                                 |
| ------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `statsmodels` | inference workflow  | coefficients, standard errors, p-values, confidence intervals, residual diagnostics, regression tables            |
| `sklearn`     | prediction workflow | train/test splits, cross-validation scores, pipelines, preprocessing, hyperparameter tuning, held-out performance |

