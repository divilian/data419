[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "wnba-labs"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "nba-api",
    "pandas",
    "polars",
    "pyarrow",
]

[tool.setuptools]
packages = ["wnba"]
